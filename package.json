{
  "devDependencies": {
    "html-minifier-next": "^3.0.0",
    "serve": "^14.2.5"
  },
  "scripts": {
    "build": "rm -rf dist && html-minifier-next --config-file html-minifier-next.json --input-dir src --output-dir dist && cp -R ./dcvjs-umd ./dist/.",
    "start": "npx serve dist"
  }
}
